# ── KagoRoute Dashboard ─────────────────────────────────────────────────
# Copy to .env.local to override defaults for local dev.

# Base URL of the KagoRoute engine. Inlined at build time for production
# (see Dockerfile ARG NEXT_PUBLIC_ENGINE_URL).
NEXT_PUBLIC_ENGINE_URL=http://localhost:8080
